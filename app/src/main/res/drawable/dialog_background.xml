<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Màu nền -->
    <corners android:radius="16dp" />      <!-- Bo góc với bán kính 16dp -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0" />         <!-- Đường viền mờ, nếu cần -->
</shape>
