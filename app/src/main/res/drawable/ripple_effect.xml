<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Xác định màu gợn sóng (highlight) -->

    <!-- Màu nền của Button khi chưa bấm -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- Màu nền của button khi không nhấn -->
            <corners android:radius="12dp"/> <!-- Bo tròn các góc của button -->
        </shape>
    </item>
</ripple>
